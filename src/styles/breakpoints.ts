export const smallMobileBreakpoint = 425;
export const mobileBreakpoint = 768;
export const tabletBreakpoint = 1024;
export const computerBreakpoint = 1280;
export const largeScreenBreakpoint = 1440;

export const maxSmallMobile = `(max-width: ${smallMobileBreakpoint}px)`;
export const maxMobile = `(max-width: ${mobileBreakpoint}px)`;
export const maxTablet = `(max-width: ${tabletBreakpoint}px)`;
export const maxComputer = `(max-width: ${computerBreakpoint}px)`;
export const maxLargeScreen = `(max-width: ${largeScreenBreakpoint}px)`;
