// import { Competition, CompetitionConstructor } from "../competition";

// type ICodWarzoneCompetitionConstructor = CompetitionConstructor;

// export class CodWarzoneCompetition extends Competition {
//   constructor({ id, duration, startsAt, state, socket }: ICodWarzoneCompetitionConstructor) {
//     super({ id, duration, startsAt, state, socket });
//   }
// }
export {};
